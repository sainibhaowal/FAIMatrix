# FAIM System Report — July 2026

## Executive Summary
FAIM is a real deterministic retrieval-and-reasoning system with:
- a native vector spine
- ConceptNet-backed lexical broadening
- a multi-million-term semantic registry runtime
- graph-scoped canonical, multilingual, and domain-memory lexicons
- semantic signatures
- deterministic reranking
- adaptive bounded hops
- a pulse-v2 cognitive pulse proof layer for FIG View

## 1. Semantic architecture
**Old overstated story:** a single giant `1M+ semantic registry` doing
everything.

**Truthful current story:** multiple real semantic layers working together.

- **ConceptNet archive:** large lexical broadener
- **Semantic registry runtime:** `2.31M+` shipped term base before graph-local
  growth
- **Cortex router:** compact deterministic task router for reasoning-mode
  selection

## 2. Retrieval and reasoning spine
FAIM’s real runtime stack is:
1. canonicalization and cleanup
2. weighted semantic expansion
3. native vector and lexical retrieval
4. graph expansion
5. native late interaction
6. deterministic reranker v2
7. adaptive bounded traversal when needed
8. pulse-v2 reason ledger emission for FIG proof surfaces
9. answer synthesis with provenance

## 3. Domain and semantic memory
FAIM now also learns graph-local domain memory from uploaded content:
- domain lexicon rows
- paraphrase and concept bundles
- graph-linked semantic memory evidence

This improves long-tail retrieval without replacing the trusted deterministic
core.

## 4. Truthful claims
You can truthfully say:
- FAIM has a real semantic registry runtime
- FAIM has a real lexicon stack
- FAIM has real ConceptNet-backed lexical recall
- FAIM has real canonical semantics
- FAIM has real domain memory
- FAIM has real semantic signatures
- FAIM has a real deterministic reranker
- FAIM has real adaptive bounded hops
- FAIM has a real pulse-v2 cognitive pulse proof layer for graph/query explain
  payloads and FIG View visualization

Do not say:
- `the registry is exactly 1,048,576 nodes`
- `the Cortex router itself is multi-million`
- `all reasoning comes from one registry layer`
- `pulse-v2 is a separate always-on websocket telemetry service`

## 5. Status
FAIM is not a fake wrapper and not plain RAG. It is a composed deterministic
retrieval-and-reasoning runtime with multiple native semantic layers that are
now documented more truthfully than before.

---
*Status: Documentation reconciled to current runtime truth*
